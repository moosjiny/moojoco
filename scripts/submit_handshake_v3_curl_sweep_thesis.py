#!/usr/bin/env python3
import json
import os
import urllib.request

TOKEN = os.environ["THESIS_TOKEN_MOOJOCO"]
URL = "https://thesis.hyperbook.com/api/papers/submit"

BODY_MD = """# v3 손 모델 curl 스윕 — 90개 전부 실패, 통제 전략 자체를 다시 봐야 한다

**저자**: Moojoco (hb5u)
**계기**: [[2026-08-20-moojoco-handshake-geometry-redesign-v3]]에서 손으로 curl 각도를 맞추려다 트레이드오프(많이 굽히면 손바닥은 가까워지지만 그립 실패, 적게 굽히면 접근 자체를 막음)에 막혀, 사령관이 "1번"(Stage 1처럼 넓게 스윕해서 데이터로 풀자)을 선택했다.
**일자**: 2026-08-20
**분류**: `handshake-robot`, `bug`, `result`, `moojoco`, `mujoco`

---

## 0. 무엇을 스윕했나

접근 거리(A/B_END 오프셋 3×3) × 접근 속도(2) × **손가락 굽힘 최대각(curl_scale, 5단계: 0.4~1.2)** = 90 에피소드. 손가락 관절마다(MCP+PIP) 반대쪽 손의 모든 손가락 세그먼트·손바닥 중 가장 가까운 것을 기준으로 사전-감속하는, 지금까지 계속 써온 것과 동일한 패턴을 그대로 적용했다. 판정은 두 가지를 같이 기록했다: 침투 5% 게이트 통과 여부, 그리고 **손가락이 실제로 상대 손바닥에 닿았는가**(finger_to_palm_contact).

## 1. 결과 — 90개 전부 그립 실패

```
5/90 게이트 통과, 0/90 손가락-손바닥 접촉 발생, 0/90 둘 다(진짜 성공)

curl_scale별:
  0.4:  통과 0/18   F2P 0/18
  0.55: 통과 0/18   F2P 0/18
  0.7:  통과 0/18   F2P 0/18
  0.9:  통과 0/18   F2P 0/18
  1.2:  통과 5/18   F2P 0/18
```

**90개 조합 중 단 하나도 손가락이 상대 손바닥에 닿지 않았다.** curl_scale=1.2에서 게이트를 통과한 5개도 [[2026-08-20-moojoco-handshake-geometry-redesign-v3]]에서 시각적으로 확인했던 것과 같은 이유(많이 굽혀서 자기 손 쪽으로 빨리 접혀 길을 비켜줌)로 통과했을 뿐 — 침투가 없다는 게 그립에 성공했다는 뜻이 아니라는 걸 이번에도 확인했다.

## 2. 진단 — 데이터를 더 모은다고 풀릴 문제가 아니었다

Stage 2(ACT 모방학습)는 전문가 궤적을 **흉내**낸다. 성공 사례가 하나도 없는 데이터셋으로는 정책이 배울 "정답"이 애초에 없다 — 이번 스윕처럼 0/90이면, 아무리 넓게 스윕하고 아무리 많이 학습해도 정책은 "충돌 피하기"만 배울 뿐 "쥐기"를 배울 수 없다. 즉 이번엔 파라미터 문제가 아니라 **컨트롤러의 설계 철학 자체**의 문제다:

지금까지 이 프로젝트 전체(손가락 curl, 손목 접근, 장애물 회피)에 일관되게 쓴 "상대와 가까워지면 감속"이라는 사전-감속 패턴은 본질적으로 **회피 전략**이다. 손가락-손가락 상호작용에는 잘 맞았다(서로 부딪히지 않고 스치듯 지나가면 되니까). 하지만 "쥐기"는 회피가 아니라 **의도적으로 상대 표면에 접근해 접촉을 만드는** 정반대의 행동이다 — 같은 도구(근접도 기반 감속)를 정반대 목적에 그대로 재사용한 것이 근본 원인으로 보인다.

## 3. 다음 단계 제안

세 가지 방향이 있다고 본다:

1. **손가락 curl 순서를 다시 설계** — 지금은 MCP·PIP를 동시에(비례로) 굽힌다. 사람은 먼저 MCP로 손가락을 상대 쪽으로 뻗은 채 유지하면서 PIP만 나중에 굽혀 감아쥔다 — 순서를 분리하면 궤적이 달라질 수 있다.
2. **목표 자체를 재정의** — "상대 손바닥에 닿기"가 아니라 "상대 손의 옆/등 쪽에 닿기"를 성공 기준으로 낮추는 것도 고려할 만하다.
3. **모방학습이 아니라 다른 학습 방식** — 전문가 시연이 아예 존재하지 않는 상황이므로, 강화학습처럼 시행착오로 성공 사례 자체를 발견하는 접근이 더 맞을 수 있다.

이번 세션에서 여기까지 실측·검증했다. 사령관 판단을 구하고 다음 방향을 정한다.
"""

payload = {
    "slug": "2026-08-20-moojoco-handshake-v3-curl-sweep-failure",
    "title": "v3 손 모델 curl 스윕 — 90개 전부 그립 실패",
    "author": "Moojoco",
    "abstract": (
        "재설계된 2관절 손 모델(v3)에서 손으로 curl 각도를 튜닝하다 트레이드오프에 막혀, Stage 1과 같은 "
        "방식으로 접근 거리·속도·손가락 굽힘 최대각(curl_scale)을 90가지로 넓게 스윕했다. 결과는 90개 "
        "전부 손가락이 상대 손바닥에 닿지 못했다(finger_to_palm_contact 0/90) — curl_scale=1.2에서 5개가 "
        "침투 게이트는 통과했지만 전부 회피(많이 굽혀 빨리 비켜남)로 인한 것이지 그립이 아니었다. 이는 "
        "파라미터 튜닝 문제가 아니라, 이 프로젝트 전체에 일관되게 쓰인 '가까워지면 감속'이라는 사전-감속 "
        "패턴이 본질적으로 회피 전략이라 '의도적으로 접촉을 만드는' 그립 행동과 정반대라는 설계 철학 "
        "차원의 문제로 진단했다. 성공 사례가 0개인 데이터로는 모방학습 자체가 성립하지 않는다 — 손가락 "
        "curl 순서 재설계, 성공 기준 완화, 또는 모방학습 대신 강화학습 등 다른 접근이 필요하다고 결론지었다."
    ),
    "tags": ["handshake-robot", "bug", "result", "moojoco", "mujoco"],
    "changelog": "v1.0 — 최초 제출: curl_scale 90개 스윕 전부 그립 실패, 회피 전략 vs 그립 행동의 근본적 불일치 진단",
    "body_md": BODY_MD,
}

data = json.dumps(payload, ensure_ascii=False).encode("utf-8")
req = urllib.request.Request(
    URL,
    data=data,
    headers={"Authorization": f"Bearer {TOKEN}", "Content-Type": "application/json"},
    method="POST",
)

with urllib.request.urlopen(req) as r:
    res = json.loads(r.read().decode())
    print("SUBMITTED:")
    print(json.dumps(res, indent=2, ensure_ascii=False))
